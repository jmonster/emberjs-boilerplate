HISTORY
=======

### v0.1.4
* Prepend md5 hash to static files for cache busting
* Move vendor script build tags so scripts don't have to be duplicated
* Copy `public` to `.temp` directory
* Add default iOS Web Clip icon
* Add `charset`, `X-UA-Compatible`, and `viewport` meta tags

### v0.1.3
* Wrap each JS file in self-invoking function before concatenating
* Fix production builds — compile templates before copying to `dist`
* Change `LICENSE` to a Markdown file

### v0.1.2
* Update README with better "Getting Started" instructions and Grunt task descriptions
* Compile Handlebars templates

### v0.1.1
* Watch for changes and rerun applicable tasks
* LiveReload HTML, CSS, and JavaScript in the browser

### v0.1.0
* First release
